# Web UI (the primary interface)
flask>=3.0

# Optional: Microsoft Graph API integration (Secure Score import).
# requests powers the device-code / client-secret / interactive flows,
# msal is only needed for certificate-based auth.
requests>=2.28
# msal>=1.20
